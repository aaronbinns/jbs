<?xml version="1.0"?>

<!-- 
  This configuration file can be used for sending documents
  to a Solr server.
 -->
<configuration>
  <property>
    <name>jbs.outputformat.class</name>
    <value>org.archive.jbs.solr.SolrOutputFormat</value>
  </property>
  <property>
    <name>jbs.solr.bufSize</name>
    <value>100</value>
  </property>
  <!-- Set this property to force every document have a 
       specific 'collection' property value.  It can also
       be set on the command-line with
         -Djbs.solr.collectionHack=foo
       which is easier when running scripts.
  <property>
    <name>jbs.solr.collectionHack</name>
    <value></value>
  </property>
  -->
</configuration>
